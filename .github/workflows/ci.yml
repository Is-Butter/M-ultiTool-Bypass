name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build with MSBuild
        run: msbuild M-ultiTool-Bypass.sln /p:Configuration=Release
